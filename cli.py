import config
from turkic.cli import handler
import turkic.database
import models

@handler("Add videos to be labeled.")
def load(args):
    pass

@handler("Output all job data.")
def dump(args):
    pass
